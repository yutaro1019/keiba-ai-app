# keiba-ai-app

競馬AIの予想・買い目提案・シミュレーション・情報更新をWeb画面から使えるアプリです。

## モデル・予想精度

### LambdaRank v2（推奨モデル）

LambdaRankによる直接順位最適化モデル。学習データ2017〜2024年、検証データ2025年。

| 指標 | 数値 |
|---|---:|
| 学習データ | 2017〜2024年 |
| 検証データ | 2025年（未使用ホールドアウト）|
| 予想1位の単勝的中率 | **44.5%** |
| 予想1位の複勝的中率 | **66.9%** |
| WIN AUC | **0.8735** |
| 特徴量数 | 50（重要度上位） |
| モデル数（アンサンブル） | 3シード |

### 旧バイナリモデル（market / no_market系）

| 指標 | market | no_market v10 |
|---|---:|---:|
| 予想1位の単勝的中率 | 33.2% | 33.1% |
| 予想1位の複勝的中率 | 63.2% | 62.9% |
| WIN AUC | 0.8370 | 0.8246 |

## 予想スタイル

### スタイル比較

カバレッジ = 買い目が1点以上発生したレースの割合（conf=0・自信度フィルターなし）。ROIはconf≥75適用・2025年ホールドアウトデータ（3,455レース）での実測値。

| スタイル | 対象券種 | カバレッジ（LambdaRank v2） | カバレッジ（market） | ROI実績（2025・conf≥75） |
|---|---|---|---|---|
| **Kelly AI** | 馬連・馬単 | ~88% | — | **+43.9%** |
| **期待値重視** | 単勝・複勝・ワイド | ~100% | ~29% | **+59.9%**（912レース） |
| **的中率重視** | 複勝・ワイド | ~100% | ~78% | **+37.4%**（912レース） |
| **自動選択** | 単勝・複勝・ワイド | ~100% | ~82% | **+70.5%**（743レース） |

> LambdaRank v2 はオッズ特徴量を使わないため、conf=0ではほぼ全レースでEV+の買い目が生まれる。  
> conf≥75フィルターを適用すると参加レースが絞られるが、ROIが大幅に改善する（下記各スタイル参照）。

---

### Kelly AI（馬連・馬単）

LambdaRank v2 専用。モデル確率とマーケットオッズを比較し、期待値10%以上の馬連・馬単のみKelly基準で資金配分します。

| 項目 | 内容 |
|---|---|
| 対象券種 | 馬連・馬単 |
| EV閾値 | マーケット比 +10% 以上 |
| カバレッジ | ~88%（LambdaRank v2） |
| 最大買い目 | 上位4頭の組合せ（top_k=4） |
| 対応モデル | LambdaRank v2 専用 |
| ROI実績 | **+43.9%**（2025年 3,455レース・フラット1,000円/ベット） |
| Kellyバンクロール | 100,000円（推奨） |

**バックテスト（2025年 3,455レース・フラット1,000円/ベット）**

| 券種 | ベット数 | 的中率 | ROI |
|---|---:|---:|---:|
| 馬連 | 9,428 | 3.5% | +39.5% |
| 馬単 | 18,987 | 1.8% | +46.1% |
| **合計** | 28,415 | 2.4% | **+43.9%** |

仕組み: LambdaRankスコア → Plackett-Luce softmax → 単勝オッズからマーケット確率を逆算 → `f* = (モデル確率 × オッズ - 1) / (オッズ - 1)` でKelly係数を算出。予算上限を超えた場合は比例縮小。

---

### 期待値重視（roi_focus）

単勝・複勝・ワイドに絞り、EV（推定期待値）が高い買い目のみを選択します。長期的な回収率プラスを優先するモードです。

| 項目 | 内容 |
|---|---|
| 対象券種 | 単勝・複勝・ワイド |
| EV閾値 | 単勝100%・複勝95%・ワイド98% 以上 |
| カバレッジ | ~100%（LambdaRank v2, conf=0）/ ~29%（market） |
| 最大買い目 | 5点・合計8ポイント以内 |
| オッズ上限 | 単勝18倍・複勝7倍・ワイド18倍 |
| 対応モデル | 全モデル対応 |
| ROI実績 | **+59.9%**（conf≥75・2025年・912レース） |

**シミュレーション実績（LambdaRank v2・2025年ホールドアウト・予算3,000円/レース）**

| conf設定 | 参加レース | カバレッジ | ROI | 収益 |
|---|---:|---:|---:|---:|
| conf=0（全レース） | 3,450 | 99.9% | 102.7% | +277,578円 |
| conf≥75（推奨） | 912 | 26.4% | **159.9%** | +1,639,526円 |

> ※ 2025年はモデル未使用のホールドアウトデータ。実払い戻しデータを使用。

---

### 的中率重視（hit_focus）

複勝・ワイドに特化し、当てやすい買い目を優先するモードです。ROIより的中回数を重視します。

| 項目 | 内容 |
|---|---|
| 対象券種 | 複勝・ワイド |
| EV閾値 | 複勝70%・ワイド80% 以上 |
| カバレッジ | ~100%（LambdaRank v2, conf=0）/ ~78%（market） |
| 最大買い目 | 5点・合計7ポイント以内 |
| オッズ上限 | 複勝7倍・ワイド18倍 |
| 対応モデル | 全モデル対応 |
| ROI実績 | **+37.4%**（conf≥75・2025年・912レース） |

**シミュレーション実績（LambdaRank v2・2025年ホールドアウト・予算3,000円/レース）**

| conf設定 | 参加レース | カバレッジ | ROI | 収益 |
|---|---:|---:|---:|---:|
| conf=0（全レース） | 3,455 | 100.0% | 98.9% | -110,991円 |
| conf≥75（推奨） | 912 | 26.4% | **137.4%** | +1,024,016円 |

> ※ 2025年はモデル未使用のホールドアウトデータ。実払い戻しデータを使用。

---

### 自動選択（smart）

自信度に応じて期待値重視と的中率重視を自動で切り替えます。自信度の高いレースに集中して参加することでROIを底上げするモードです。

| 項目 | 内容 |
|---|---|
| 対象券種 | 単勝・複勝・ワイド（自動選択） |
| 高自信度（≥75） | 期待値重視（roi_focus）を優先適用 |
| 低自信度（＜75） | スキップ（デフォルト） |
| カバレッジ | ~22%（LambdaRank v2・conf≥75デフォルト・743/3,455レース）|
| 最低自信度 | 75（Webアプリから変更可能） |
| 対応モデル | 全モデル対応 |
| ROI実績 | **+70.5%**（conf≥75・2025年・743レース） |

**シミュレーション実績（LambdaRank v2・conf≥75・予算3,000円/レース）**

| 年 | 参加レース | ROI | 収益 |
|---|---:|---:|---:|
| 2023 | 1,342 | 175.3% | +3,031,000円 |
| 2024 | 1,314 | 195.0% | +3,743,285円 |
| 2025（ホールドアウト） | 743 | **170.5%** | +1,572,312円 |

> ※ 2025年はモデル未使用のホールドアウトデータ。実払い戻しデータを使用。

## モデル切り替え

| モデル | 説明 | 推奨用途 |
|---|---|---|
| `no_market_lambdarank_v2` | LambdaRank v2（**推奨**） | Kelly資金分配との組み合わせ |
| `market` | 通常モデル（オッズ・人気あり） | 単純な人気馬選び |
| `no_market_v10` | バイナリモデル v10 | 旧来互換 |

CLIでは `--model-variant no_market_lambdarank_v2`、Webアプリでは「予想モデル」から切り替えます。

## 使い方

### Webアプリ起動

```powershell
cd C:\Users\yukim\Downloads\keiba_ai_package\keiba_ai
python -X utf8 src\web_app.py --host 127.0.0.1 --port 7860
```

ブラウザで `http://127.0.0.1:7860` を開きます。

### Kelly資金分配バックテスト

```powershell
cd keiba_ai
python src\backtest_kelly.py --years 2025 --kelly 0.25 --min-ev 0.1 --top-k 4 --tickets umaren umatan --flat-bet 1000
```

### バックテストオプション

| オプション | デフォルト | 説明 |
|---|---|---|
| `--years` | 2025 | 評価年（2025のみ推奨: 学習外データ）|
| `--kelly` | 0.25 | Kelly係数の倍率 |
| `--min-ev` | 0.0 | 最低期待値フィルタ（0.1推奨）|
| `--top-k` | 5 | 組合せ馬券の対象上位頭数（4推奨）|
| `--tickets` | 全券種 | 対象券種 |
| `--flat-bet` | 0 | 固定額ベット（0=Kelly複利）|

### Python API（predictor連携）

```python
from predictor import KeibaPredictor
from kelly_betting import load_odds_live

predictor = KeibaPredictor(model_variant="no_market_lambdarank_v2")
race_df = ...  # 出走馬データ
odds_data = load_odds_live(race_id, data_dir="data/")

# Kelly推薦馬券を取得
bets = predictor.recommend_bets(
    race_df,
    odds_data,
    bankroll=100_000,
    kelly_factor=0.25,
    min_ev=0.1,
    top_k=4,
    ticket_types=["umaren", "umatan"],
)

for b in bets:
    print(f"{b['ticket_kind']} {b['horses']}  "
          f"EV={b['ev']:+.1%}  掛け金={b['bet_amount']:,}円")
```

### CLIバックテスト（バイナリモデル旧来）

```powershell
python -X utf8 src\keiba_ai.py --no-banner --backtest --year 2025 --budget 3000 --style hybrid --model-variant market
```

## 主な機能

- 未来レース予想（LambdaRank v2 / market モデル対応）
- Kelly基準による馬連・馬単の資金配分推薦（ROI +43.9% 実績）
- 日付指定で全レース予想
- 競馬場とR指定で個別レース予想
- 予想順位・自信度・推定勝率・推定3着内率の表示
- 期間指定シミュレーション
- 情報更新モード（スクレイピング＋lookupテーブル更新）
- 実払い戻しデータを使った検証

## 情報更新とデータについて

スクレイピングで取得したデータは、netkeiba等の利用規約に配慮するため、このリポジトリでは公開しません。必要な場合は各自の環境で情報更新モードを実行して取得してください。

情報更新モードでは、新しく追加されたレース結果を学習用データと同じ形式に整え、LambdaRank v2モデルのlookupテーブルも更新します。

## 注意

このアプリの予想、勝率、自信度、買い目、回収率は参考値です。実際の馬券購入は自己責任で行ってください。
